package interpreter.interpreter;

public class TerminalOperacion extends AbstractExpresion{
	@Override
	public void interpreter(Context context, int valor) {
		if (context.input.startsWith("suma") || context.input.startsWith("resta")){
            
        }
	}

}
